server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Si el usuario refresca una ruta virtual, lo manda al index.html
        try_files $uri $uri/ /index.html;
    }

    error_page 404 /index.html;
}